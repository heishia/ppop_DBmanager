# Application
APP_ENV=dev

# Database (PostgreSQL)
DATABASE_URL=

# JWT
JWT_SECRET=

# Email (SMTP)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
SMTP_FROM=

# API
API_PORT=3001
API_URL=http://localhost:3001

# GCP (Gemini AI) - AI 데이터 변환에 사용
GCP_PROJECT_ID=
GCP_LOCATION=asia-northeast3
# 프로덕션 배포 시: 서비스 계정 JSON 키 전체를 문자열로 입력
# 로컬 개발 시: gcloud auth application-default login 사용
GOOGLE_APPLICATION_CREDENTIALS_JSON=
