# Application
APP_ENV=dev

# Database (PostgreSQL)
DATABASE_URL=

# JWT
JWT_SECRET=

# Email (SMTP)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
SMTP_FROM=

# API
API_PORT=3001
API_URL=http://localhost:3001
